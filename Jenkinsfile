pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hello world! This is vishnu' "Tesing with the webhook triggers"
            }
        }
    }
}
